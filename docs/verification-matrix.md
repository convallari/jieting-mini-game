# 当前复刻验证矩阵

## 自动检查

运行 `npm run check` 应同时通过：

- 通用资源清单检查；
- Spine 资源检查（当前 54 个动画）；
- Spine 运行时映射检查；
- 战斗数值检查（当前 19 组 canonical checks）。
- 街亭主题与动画预设配置检查。

运行 `npm run build` 验证正式 Vite 构建。

## 流程检查

- 主菜单标题“街亭之战”，“开始游戏”进入第 1 波，显示完整 8×10 单玩家地图；
- 战斗顶部显示关卡“街亭”和资源“军粮”；
- 地图能看到山地、水源、蜀营标记；
- 马谡、王平、诸葛亮、张郃、司马懿、魏军先锋字形能正常显示；
- 特定波次触发“汲道”压力，汲道被突破后军粮收益下降；
- 排行榜：显示本地持久化最高波次，可返回；
- 武器背包：显示刀/弓/枪/骑/铲牌池数量及农民能力状态，可返回；
- 暂停：显示遮罩和继续提示，Spine 覆盖层隐藏；
- 页面切后台：自动进入暂停状态，返回后需主动继续；
- 桌面快捷键：主菜单按 `Enter` 开始，战斗按 `Esc` 暂停/继续；
- 继续：Spine 覆盖层恢复；
- 失败：显示“街亭失守”、坚持波次和“再来一局”；
- 胜利：调试入口 `?debugAttack=1&debugEnd=win` 显示“守住街亭”结算；
- 重开：结算页点击“再来一局”创建干净的新局状态。
- 排行榜记录：进入新波次后持久化最高波次，重新打开菜单仍可读取；
- 砚台区域：效果持续5秒，后来进入半径1.5格的敌军同样减速20%。

## 视口检查

- 桌面：`1280×720`；
- 移动：`390×844`；
- 两种尺寸均保留完整地图、上下两条路线、单一营地和共享道具栏；
- 最近一次调试战斗页控制台错误数为 0，剩余仅 Spine FastRender 性能警告；favicon 已本地化，不再有资源404。

首领精确还原与将领动画补齐按当前目标明确放弃，不纳入本矩阵的通过条件。
